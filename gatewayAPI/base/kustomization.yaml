apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - gateway.yaml
  # - kyverno-policy.yaml  # requires Kyverno CRDs; enable once Kyverno is installed
  - networkpolicy.yaml
