apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - pvc.yaml
  - networkpolicy.yaml
  # actual-server-secret is NOT listed here — it must be applied out-of-band.
  # See secret.yaml.example and README.md §Secret Management.
